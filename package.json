{
  "name": "totp-local-manager",
  "version": "1.0.0",
  "description": "Local password, TOTP, and session manager",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.19.2",
    "otplib": "^12.0.1",
    "playwright": "^1.53.0"
  }
}
